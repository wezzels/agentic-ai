"""
Test configuration for Agentic AI tests.
"""

import sys
from pathlib import Path

# Add project root to Python path (absolute path from current dir)
sys.path.insert(0, str(Path('.').absolute()))
